# NEXT CHANGELOG

## Release v1.103.0

### Breaking Changes

### New Features and Improvements

* Added support for `azure_openai` provider in `databricks_model_serving` resource with a dedicated `azure_openai_config` block. This allows for explicit configuration of Azure OpenAI endpoints, including `openai_api_base`, `openai_api_version`, and `openai_deployment_name` ([#xxxx](https://github.com/databricks/terraform-provider-databricks/pull/xxxx)).

### Bug Fixes

### Documentation

### Exporter

### Internal Changes
